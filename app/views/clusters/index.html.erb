<h3>Listing clusters</h3>

<table>
  <tr>
	<th>Name</th>
    <th></th>
    <th></th>
  </tr>

<% @clusters.each do |cluster| %>
  <tr class="<%= cycle("even", "odd") %>">
    <td><%= link_to cluster.name, cluster %> (<%= cluster.nodes.size %>)</td>
    <td><%= link_to 'Edit', edit_cluster_path(cluster) %></td>
    <td><%= link_to 'X', cluster, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Cluster', new_cluster_path %>
